{
  "name": "@pma/matching",
  "version": "0.0.0",
  "private": true,
  "description": "Same-question matching engine (rules -> embeddings -> calibration -> resolution alignment).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@pma/core": "*",
    "@pma/storage": "*"
  }
}
