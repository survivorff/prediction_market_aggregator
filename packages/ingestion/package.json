{
  "name": "@pma/ingestion",
  "version": "0.0.0",
  "private": true,
  "description": "Ingestion orchestrator, schedulers, pollers, WS managers, upsert writers.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "start": "node dist/main.js",
    "dev": "node --watch dist/main.js"
  },
  "dependencies": {
    "@pma/adapters": "*",
    "@pma/core": "*",
    "@pma/matching": "*",
    "@pma/storage": "*"
  }
}
